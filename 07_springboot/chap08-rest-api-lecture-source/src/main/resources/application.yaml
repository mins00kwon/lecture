server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 10MB     # 파일 한 개당
      max-request-size: 10MB  # 요청 당(파일이 여러개라도)
  web:
    resources:
      static-locations: /Users/mk/uploadFiles